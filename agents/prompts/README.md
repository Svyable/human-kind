# Agent prompts

Store versioned prompts here only when they implement a defined role or workflow. Prompts must reference `docs/agent-protocol.md`, state the active role, require source disclosure, and preserve human review.

Treat prompts as testable program artifacts: changes should be reviewed against cases in `agents/evals/`.
