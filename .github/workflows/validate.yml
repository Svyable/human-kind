name: Validate commons

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: "17 9 * * 1"

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Install validation dependencies
        run: python -m pip install --disable-pip-version-check pyyaml jsonschema
      - name: Run agent harness regression suite
        run: python scripts/test_agent_harness.py
      - name: Test conflict-resistant intake index helper
        run: python scripts/test_index_intake_fragment.py
      - name: Test fragment-aware agent work queue validation
        run: python scripts/test_validate_agent_work_queue_fragments.py
      - name: Validate machine-readable agent work queue
        run: python scripts/validate_agent_work_queue_fragments.py
      - name: Validate pending intake queue state
        run: python scripts/validate_pending_intake.py
      - name: Validate conflict-resistant idea-index fragments
        run: python scripts/validate_idea_index_fragments.py
      - name: Validate dossiers, structured reviews, review freshness, and local links
        run: python scripts/validate_ideas.py
      - name: Check external links
        uses: lycheeverse/lychee-action@v2
        with:
          args: --no-progress --exclude-path .git --exclude "^mailto:" "**/*.md"
          fail: true
